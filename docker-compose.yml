version: '3.8'

services:
  backend:
    build: .
    ports:
      - "8001:8001"
    restart: always
    environment:
      - PYTHONUNBUFFERED=1
    # The application reads from config.properties which is generated by the workflow
